# Batch IDE

This is a small IDE / interpreter for some parts of cmd.exe. 

It understands:

  - choice
  - cls
  - color
  - echo
  - exit
  - goto
  - if
  - pause
  - rem
  - set
  - timeout
  - title
